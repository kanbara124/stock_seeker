# 股票信息智能调研系统 —— 项目需求文档

版本：v1.0（草案）
文档性质：个人学习项目需求说明
声明：本项目仅用于技术学习与研究，产出内容不构成任何投资建议。

---

## 一、项目背景与目标

### 1.1 背景

调研一只股票时，信息分散在行情软件、公司公告、财经新闻、券商研报、股民论坛等多个渠道，人工搜集和阅读耗时较长，且难以追溯每条结论的出处。

### 1.2 目标

搭建一套自动化调研流水线：用户输入股票代码或公司名称，系统自动完成「多渠道信息采集 → 大模型阅读理解 → 汇总生成研究报告」，并满足两个硬性要求：

1. **可溯源**：报告中每个事实性结论都标注信息来源，来源可点击跳转到原始网页或文件。
2. **可控成本**：单份报告的 API 调用成本控制在可接受范围（目标 < 1 元）。

### 1.3 明确不做什么（范围边界）

- 不做实时交易、不做选股推荐、不做涨跌预测；
- 不做 Web 前端（v1.0 命令行运行即可，报告以 Markdown/PDF 文件交付）；
- v1.0 只覆盖 A 股，不考虑港股、美股。

---

## 二、功能需求

### 2.1 核心流程（Pipeline）

```
输入股票代码
  → ① 结构化数据采集（akshare：行情、财务、公司概况）→ 直接写入报告"财务表现"章节，不入素材库
  → ② 按分档配额多渠道链接搜集（T1 公告 / T3 新闻 / T4 研报与论坛，详见 2.4）
  → ③ 网页正文抓取与清洗（含 PDF 解析）
  → ④ 预处理（chat 模型摘要 / 去重 / 股吧情绪聚合 / 研报评级过滤，详见 2.5）
  → ⑤ 建立带编号的素材库（每条素材登记 {编号, 档位, 来源, 标题, URL, 摘要}）
  → ⑥ reasoner 模型汇总生成报告（按分档规则引用素材编号）
  → ⑦ 编号映射回 URL，输出 Markdown / PDF 报告
```

### 2.2 功能清单

| 编号 | 功能 | 说明 | 优先级 |
|---|---|---|---|
| F1 | 结构化数据获取 | 用 akshare 拉取公司概况、日线行情、财务摘要、个股新闻列表 | P0 |
| F2 | 关键词搜索 | 按预设关键词模板（财报/研报/最新消息/讨论）搜索并收集链接 | P0 |
| F3 | 正文提取 | 静态网页用 trafilatura 提取正文，过滤广告导航等噪音 | P0 |
| F4 | 素材编号体系 | 每段素材分配唯一编号，记录 {编号, 来源渠道, 标题, URL, 正文} | P0 |
| F5 | LLM 汇总 | 调用 DeepSeek API，按固定结构生成报告，结论后标注 [编号] | P0 |
| F6 | 报告生成 | 输出 Markdown，文末附来源清单（编号→标题→URL），可用 pandoc 转 PDF | P0 |
| F7 | 动态页面抓取 | 雪球、股吧等 JS 渲染页面用 Playwright 渲染后再提取 | P1 |
| F8 | 研报 PDF 解析 | 用 PyMuPDF 提取研报文字后进入素材库 | P1 |
| F9 | 网页缓存 | 抓取结果缓存到本地，调试 prompt 时不重复爬取 | P1 |
| F10 | 多 Agent 编排 | 用 LangGraph 等框架拆分"搜集/阅读/写作"角色 | P2 |

### 2.3 报告结构要求

生成的调研报告固定包含以下章节：

1. 公司概况（主营业务、所属行业、市值规模）
2. 财务表现（近期营收、利润及同比变化）
3. 近期动态（公告、新闻事件）
4. 市场观点与分歧（研报观点、论坛讨论的多空两方）
5. 风险提示
6. 信息来源清单

### 2.4 信息源分档与抓取配额

按可信度将信息源分四档，每档在 pipeline 中承担不同角色。以下配额为 v1.0 默认值，可在 `config.py` 中调整。

**四档源清单**：

- **T1 官方与监管**（最高权威，公司事实的金标准）
  - 巨潮资讯网（公告、财报原文）
  - 沪深北交易所（公告、监管问询函）
  - 证监会（政策法规、处罚决定）
  - 国家统计局 / 央行 / 财政部（宏观数据）
- **T2 专业数据平台**（结构化数据）
  - 东方财富 + Choice（行情、财务、研报中心、公告）
  - 同花顺 iFinD、Wind（机构级，收费；Wind 可尝试北邮图书馆终端）
  - 新浪财经、腾讯自选股（行情与新闻聚合）
  - akshare 底层即接入此档
- **T3 财经媒体**（事件与解读）
  - 财新（深度调查，部分付费）
  - 四大证券报：证券时报、上海证券报、中国证券报、证券日报（法定信披媒体）
  - 一财、21 世纪经济报道、界面新闻
  - 华尔街见闻
- **T4 研报与讨论社区**（观点/情绪，非事实来源）
  - 慧博投研、东财研报中心
  - 雪球（长文观点）
  - 东财股吧（情绪指标）

**抓取与入模配额（默认）**：

| 档 | 抓取上限 | 入 LLM 素材数 | 报告角色 |
|---|---|---|---|
| T1 | 近 3 月全部公告 | 5~8 条（去重摘要后） | 事实基石 |
| T2 | akshare 一把梭 | 不入素材库，直接写入报告"财务表现"章节 | 数据基础 |
| T3 | 15~20 条 | 6~10 条 | 事件与解读 |
| T4 | 研报 5~10 / 雪球 5~10 / 股吧 30~50 | 研报 2~3 + 雪球 2~3 + 股吧聚合成 1 条情绪总结 | 观点/情绪 |

**T4 特殊处理规则（重要）**：

- **研报**：只提取行业逻辑与数据，**忽略评级结论与目标价**。因为券商几乎只发买入/增持，聚合评级会造成一边倒的假象。
- **雪球**：作为观点来源，不作为事实来源。
- **股吧**：只用于报告"市场情绪"表述，抓取后先由 chat 模型聚合成一段情绪总结（多空比例、热议话题），不将原帖作为素材编号。

**Prompt 引用约束（对应 F5）**：

- 公司概况 / 财务表现 / 近期动态章节：只允许引用 T1/T2 编号。
- 市场观点与分歧章节：可引用 T3/T4。
- 涉及情绪的表述：允许引用 T4 股吧的情绪总结。

### 2.5 LLM 分层调用策略

为控制 deepseek-reasoner 的推理 token 成本，采用两档模型分工：

| 阶段 | 模型 | 用途 |
|---|---|---|
| 预处理 | deepseek-v4-flash（非思考模式） | 单篇正文摘要（长文压缩到 800~1500 字）、跨源去重判断、研报评级过滤、股吧情绪聚合 |
| 最终汇总 | deepseek-v4-flash（思考模式） | 读取素材库全量摘要，生成结构化报告，处理多方观点对比与风险归纳 |

**成本预估（2026-07-17 官方定价回代，元/百万 tokens）**：

| 模型 | 输入(缓存命中) | 输入(未命中) | 输出 |
|---|---|---|---|
| deepseek-v4-flash | 0.02 | 1 | 2 |
| deepseek-v4-pro | 0.025 | 3 | 6 |

- 预处理阶段（Flash 非思考）：约 28 万 input + 7 万 output ≈ **0.42 元**
- 汇总阶段（Flash 思考）：约 8 万 input + 4 万 output（含推理 token）≈ **0.16 元**
- **单份报告实测预算 ≈ 0.6 元/份**，已满足 <1 元目标。
- 若汇总切换到 Pro 思考模式，成本升至 ≈ **0.9 元/份**，仍在预算内。

**策略 C（v1.0 默认）**：先全 Flash 跑通并观察报告质量；若观点对比或风险归纳不到位，只把汇总阶段切换到 v4-pro，其余不变。

若成本超预算，优先压 T3/T4 素材数量与研报截断字数；也可利用 prompt 缓存（稳定的系统前缀）把输入成本再压 50 倍。

**模型命名迁移提示**：旧别名 `deepseek-chat` / `deepseek-reasoner` 将于 **2026-07-24 15:59 UTC 废弃**；代码里请直接使用 `deepseek-v4-flash`，通过请求参数切换思考模式。

### 2.6 关键参数默认值（v1.0，待调整）

集中在 `config.py`，便于统一调整：

| 参数 | 默认值 | 说明 |
|---|---|---|
| `TIME_WINDOW_ANNOUNCEMENT` | 3 个月 | 公告抓取时间窗 |
| `TIME_WINDOW_NEWS` | 3 个月 | 新闻抓取时间窗 |
| `TIME_WINDOW_REPORT` | 6 个月 | 研报抓取时间窗 |
| `TRUNCATE_ANNOUNCEMENT` | 4000 字 | 单条公告截断长度 |
| `TRUNCATE_NEWS` | 2000 字 | 单条新闻截断长度 |
| `TRUNCATE_REPORT` | 6000 字 | 单条研报截断长度（信息密度高） |
| `REPORT_TARGET_LENGTH` | 约 5000 字 | 最终报告目标长度 |
| `REPORT_COST_BUDGET` | 1 元/份 | 实测：Flash 全流程 ≈ 0.6 元，Pro 汇总 ≈ 0.9 元 |
| `TEST_TICKERS` | 600519 + 一只 ST 股 + 一只热门题材股 | 覆盖信息全 / 信息稀疏 / 分歧多三种场景 |

---

## 三、非功能需求

- **防幻觉**：模型只允许使用素材库中的信息；素材没有的内容必须写"公开资料未提及"；来源编号由程序分配并在生成后由程序映射回 URL，不允许模型自行编造链接。
- **时效性**：报告开头标注数据截止日期；行情与新闻以近 3~6 个月为主。
- **健壮性**：单个网页抓取失败不导致整体流程中断，跳过并记录日志。
- **安全性**：API key 存放于 `.env` 文件，不写入代码、不提交 GitHub。

---

## 四、技术选型

| 环节 | 工具 | 备注 |
|---|---|---|
| 语言 / 环境 | Python 3.10+，venv 虚拟环境 | 编辑器用 VS Code |
| 结构化数据 | akshare | 免费、无需 key；接口名可能随版本变动，报错时查官方文档 |
| 正文提取 | trafilatura | 通用启发式正文识别，中文支持好 |
| 动态渲染 | Playwright（Chromium） | 处理雪球、股吧等 JS 页面 |
| PDF 解析 | PyMuPDF（import fitz） | 提取研报文字 |
| 搜索 | Tavily API | v1.0 首选，抽象出 adapter 保留切换到必应/其他源的可能 |
| 大模型 | DeepSeek API：deepseek-v4-flash（非思考=预处理 / 思考=汇总），备用 deepseek-v4-pro | 详见 2.5；旧别名 2026-07-24 废弃 |
| 报告导出 | Markdown + pandoc 转 PDF | |

---

## 五、里程碑计划

| 阶段 | 目标 | 验收标准 |
|---|---|---|
| M1 | 跑通 akshare 数据获取 | 输入 600519，打印公司概况与 10 条新闻标题 |
| M2 | 跑通正文抓取 | 把新闻正文清洗后存为本地 txt |
| M3 | 跑通 LLM 汇总 | 素材输入模型，输出一段带编号引用的总结 |
| M4 | 完整流水线 v1.0 | 一条命令生成完整 Markdown 报告，来源可溯源 |
| M5 | 增强 | 加 PDF 研报、Playwright、缓存、搜索渠道扩展 |

原则：小步快跑，每个阶段单独可运行、可验证，不要试图一次写完整个系统。

---

## 六、踩坑点清单（重点）

### 6.1 数据采集类

1. **akshare 接口名会变**：库更新快，旧教程里的函数名可能已改名或废弃。报错先去官方文档搜最新接口名，不要死磕旧代码。
2. **雪球、股吧抓不到内容**：这类页面靠 JS 动态渲染，requests 拿到的是空壳 HTML，必须先用 Playwright 渲染再提取正文。
3. **被封 IP**：高频抓取会触发反爬（403、验证码、跳转登录）。每次请求之间加 `time.sleep(1)`，带上正常浏览器的 User-Agent，失败自动重试并降速。
4. **正文里全是广告**：不要把整个 HTML 直接喂给模型，必须经 trafilatura 之类的正文提取，否则又贵又干扰判断。
5. **研报 PDF 是扫描件**：部分老研报是图片型 PDF，PyMuPDF 提取出来是空的，需要 OCR（Tesseract）兜底，v1.0 可以直接跳过这类文件。
6. **链接失效与重复**：不同渠道常转载同一篇文章，进素材库前按 URL 和标题去重；抓取失败的链接记日志后跳过。

### 6.2 大模型类

7. **幻觉引用（最危险的坑）**：绝不能让模型自己生成来源链接，它编出的 URL 格式逼真但不存在。正确做法：程序给素材编号 → prompt 强制"结论后标注 [编号]" → 程序把编号映射回真实 URL。
8. **超出上下文长度**：几十篇长文拼起来会爆 token。每篇素材截断（如 4000 字），超长研报先让模型分段摘要再汇总。
9. **成本失控**：调试阶段先用 2~3 篇素材跑通，确认 prompt 效果后再上全量；抓取结果务必本地缓存，避免改 prompt 时重复爬取和重复调用。
10. **模型"脑补"数据**：prompt 中明确"只允许使用给定资料，资料没有就写公开资料未提及"；财务数字尽量以 akshare 的结构化数据为准，而非模型转述。
11. **输出格式不稳定**：模型偶尔不遵守章节结构，可在 prompt 里给出完整的 Markdown 模板让它填空，或对输出做格式校验、失败重试。

### 6.3 工程与合规类

12. **API key 泄露**：key 写进 `.env` 并用 python-dotenv 读取，`.gitignore` 里加上 `.env`；一旦误提交到 GitHub，立刻去平台作废重置。
13. **版权与合规**：研报、新闻有版权，抓取的原文只用于个人学习，不要二次发布全文；遵守目标网站的 robots 协议，控制抓取频率。
14. **环境依赖混乱**：始终用 venv 虚拟环境，依赖写进 `requirements.txt`，换机器能一键复现。
15. **投资误导风险**：论坛和股吧内容情绪化严重、常有谣言，报告中"市场观点"章节必须明确标注其为主观讨论，且整份报告注明"不构成投资建议"。

---

## 七、目录结构建议

```
stock-research/
├── .env                  # API key（不入库）
├── .gitignore
├── requirements.txt
├── main.py               # 入口：串起整个流水线
├── config.py             # 关键词模板、模型参数
├── collector/
│   ├── fundamental.py    # akshare 结构化数据
│   ├── search.py         # 搜索与链接收集
│   └── fetcher.py        # trafilatura / Playwright / PDF 抓取
├── materials.py          # 素材库与编号体系
├── summarizer.py         # LLM 调用与 prompt
├── report.py             # 编号映射与报告生成
└── cache/                # 网页缓存
```
