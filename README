# stock_seeker

股票信息智能调研系统。输入股票代码，自动完成信息采集 → 阅读整理 → 生成调研报告。

调研一只股票要翻行情软件、公告、新闻、研报、股吧，一个渠道一个渠道看太费劲了，所以写了个脚本把这套流程跑通：机器负责搜和读，人只看最后那份报告。

## 流程

输入股票代码 → akshare 拉行情和财务数据 → 按信源分档搜集链接（公告/新闻/研报/雪球/股吧）→ 抓正文（静态页 trafilatura，JS 页 Playwright，研报 PDF 用 PyMuPDF）→ 预处理：长文摘要、跨源去重、股吧情绪聚合 → LLM 汇总成报告，每条结论标注素材编号 → 编号映射回真实 URL，输出 Markdown 报告。

报告里每个结论后面都有编号，文末有编号对应的标题和链接，点得到原始出处。素材库里没有的内容会直接写"公开资料未提及"，不让模型自己编。

## 为什么分档

不同来源的可信度差很多，混在一起喂给模型容易出问题：
- T1 官方公告（巨潮、交易所）——事实标准
- T2 数据平台（akshare 底层）——只取结构化数据
- T3 财经媒体——事件和解读
- T4 研报/雪球/股吧——只当观点看，不当事实用；券商研报只提逻辑和数据，评级和目标价直接丢掉，反正基本都是买入；股吧原文不进素材，先聚合成一段多空情绪总结

事实类章节只允许引用 T1/T2，观点和情绪才允许引用 T3/T4。

## 成本控制

全流程用 deepseek-v4-flash：预处理阶段（非思考模式）做摘要和去重，汇总阶段（思考模式）出报告。实测单份报告 API 花费约 0.6 元，控制在 1 元预算内。网页抓取结果本地缓存，调 prompt 不会重复花钱爬。

## 跑起来

git clone → 建 venv → pip install → playwright install chromium → 填 .env 里的 API key → python main.py，输入股票代码等几分钟。

详细需求说明见《股票调研系统-项目需求文档.md》。

## 声明

本项目仅用于技术学习，产出内容不构成任何投资建议。抓取的原文只用于个人学习，不二次发布。
